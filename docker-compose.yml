services:
  role-lister-bot:
    build: .
    container_name: role-lister-bot
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - role_lister_data:/app/data

volumes:
  role_lister_data:
